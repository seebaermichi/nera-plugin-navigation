include ../helper/mixins

mixin simpleNav(elements=app.nav.elements, className='', rootPath='/')
    nav(class=`${className}`)
        include ../helper/setup
            +link(item, activeClass, activePathClass)
