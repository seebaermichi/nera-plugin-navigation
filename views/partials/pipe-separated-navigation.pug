include ../helper/mixins

mixin pipeSeparatedNav(elements=app.nav.elements, className='', rootPath='/')
    nav(class=`${className}`)
        include ../helper/setup
            if index !== 0
                | &nbsp;|&nbsp;
            +link(item, activeClass, activePathClass)
